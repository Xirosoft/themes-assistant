jQuery(document).ready(function ($) {
    /**
     * Code will be write here.
     * */

    /*** Tilt JS Animation */
    if ($(".CardAnimation").length > 0) {
        $(".CardAnimation").tilt({
            reset: true,
        });
    }
    


});